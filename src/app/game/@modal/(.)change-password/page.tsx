import { Modal } from "@mui/material";

import ChangePassword from "@/app/game/(modal)/change-password/page";

export default function ChangePasswordModal() {
  return (
    <Modal
      open
    >
      <ChangePassword />
    </Modal>
  );
}
